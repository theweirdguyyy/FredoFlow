/**
 * CSV export utility for workspace data.
 * Skeleton — will be implemented with the analytics feature.
 */
export function exportToCsv(data, columns) {
  // Will be implemented in the analytics task
  return '';
}
